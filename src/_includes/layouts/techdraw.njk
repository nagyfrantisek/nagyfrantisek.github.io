---
layout: default
---

<article class="max-w-5xl mx-auto">
    <header class="mb-14">
        <h1 class="text-3xl text-center font-bold leading-normal text-slate-900 mt-0 mb-3">{{ title }}</h1>
        <div class="text-center">Létrehozva: {{ page.date | readableDate }}</div>
        {% if tags %}
        <div class="mt-3 text-center">
            {% for tag in tags %}
            <a href="{{ '/tags/' | url }}{{ tag }}" class="inline-block bg-slate-200 rounded-full px-3 py-1 text-sm font-medium text-slate-700 m-0.5">#{{ tag }}</a>
            {% endfor %}
        </div>
        {% endif %}
    </header>
    <div id="content" class="prose text-slate-800 max-w-960 m-auto">
        {{ content | safe }}
        <img class="w-full mx-auto" src="/assets/img/{{ image | url }}">
    </div>
</article>